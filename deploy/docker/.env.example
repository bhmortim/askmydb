# Copy to .env and fill in. Keep .env out of git (it holds the secret key).
ASKMYDB_SHARE_KEY=paste-a-long-random-key-here
# The machine running LM Studio, reachable from the Docker host on your LAN.
LMSTUDIO_URL=http://192.168.0.2:1234
